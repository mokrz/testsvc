---
name: Test Workflow

on:
  push:
    branches: [ master ]

jobs:
  test-job:
    runs-on: ubuntu-latest
    container:
      options: --rm
    steps:
      - name: checkout
        uses: actions/checkout@v2
